controller:
  persistence:
    enabled: false
  serviceAccount:
    create: true
    name: jenkins
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"

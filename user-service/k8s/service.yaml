apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: user-service   # ✅ 이 줄 추가!
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 80              # 클러스터 내부 포트
      targetPort: 8001      # 컨테이너 내부 Django 앱 포트
  type: ClusterIP